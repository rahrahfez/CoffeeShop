import React, { useState } from 'react';
import { Modal, View, Text } from 'react-native';

function setModalVisable(visable) {
  return
}

export default function Modal() {
  const [modalVisable, setModalVisable] = useState(false);

  

  return (
    <View></View>
  );
}